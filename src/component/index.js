
//main working of index file is to just export all the component of the folder, 
//thus if want to access any component in any folder, just import the names

import Header from "./Header/Header"
import Footer from "./Footer/Footer"

export {Header,Footer}